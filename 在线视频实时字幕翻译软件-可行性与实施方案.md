# 在线视频实时字幕翻译软件：可行性分析与实施方案

> 文档版本：v1.0  
> 编写日期：2026-08-30  
> 第一阶段平台：Windows 10 Build 20348 及以上  
> 推荐路线：指定应用音频捕获 + 云端流式识别/翻译 + 桌面悬浮字幕

## 1. 项目概述

### 1.1 产品目标

开发一款能够在用户观看在线视频时，实时接收指定应用所播放的人声音频，将其识别为原文并翻译成中文或其他目标语言，再以同步字幕形式显示的软件。

第一阶段不要求用户下载视频，也不依赖视频网站已有字幕。用户只需：

1. 打开夸克、Chrome、Edge 或其他播放器并播放视频；
2. 在本软件中选择正在发声的应用；
3. 选择源语言和目标语言；
4. 启动翻译，在悬浮窗中观看实时译文或双语字幕；
5. 会话结束后校对并导出 SRT、VTT 或 TXT。

### 1.2 核心结论

该产品在技术上可行，推荐先开发 Windows 桌面端。Windows 提供 WASAPI Process Loopback 能力，可以捕获指定进程及其子进程播放的音频，而不必读取网页 DOM、获取视频地址或要求视频网站提供接口。因此，该方案比浏览器扩展覆盖面更广，也比自带浏览器更少受到网站登录、页面改版、CORS 和播放器实现方式的影响。

推荐的第一阶段数据链路如下：

```text
目标应用音频
  → WASAPI 指定进程回环捕获
  → PCM 重采样与声道转换
  → VAD / 流式分段
  → WebSocket 上传
  → 云端实时 ASR 增量结果
  → 实时翻译增量结果
  → 临时字幕合并与稳定
  → 悬浮字幕显示
  → SRT / VTT / TXT 导出
```

推荐第一接入腾讯云实时语音识别与翻译，同时建立统一的 `ProviderAdapter`，后续可以加入 OpenAI 或其他国内云服务，避免核心业务绑定单一供应商。

## 2. “无延迟”目标如何定义

严格的零延迟不可能实现。系统至少需要收到一小段语音，才能判断音素、词语、断句和上下文；翻译还需要等待足够的语义信息。市面产品给人的“无延迟”体验，通常来自流式临时字幕：先快速显示不稳定文本，再随着上下文补全或修正。

第一阶段采用“感知无延迟”目标：

| 指标 | MVP 目标 | 说明 |
|---|---:|---|
| 首批临时原文 | ≤ 1 秒 | 从检测到语音开始计时，良好网络条件 |
| 首批临时译文 | ≤ 1.5 秒 | 允许逐词或短语更新 |
| 稳定译文 P50 | ≤ 2 秒 | 50% 的字幕片段在此时间内稳定 |
| 稳定译文 P95 | ≤ 3.5 秒 | 95% 的字幕片段在此时间内稳定 |
| UI 更新频率 | 100–250 ms | 避免逐字符刷新造成闪烁 |

这些数值是产品验收目标，不是未经测试的性能承诺。最终结果会受到源语言、说话速度、背景音乐、云服务、电脑负载和网络质量影响，必须通过真实视频测试确认。

默认采用“极速模式”，并预留三个配置档：

- 极速：最短分段和稳定窗口，优先快速出字，允许更多修订；
- 平衡：稍长上下文，降低跳字和误改；
- 高质量：等待更完整的句子，翻译更自然，但延迟更高。

## 3. 市场形态与目标定位

夸克官网公开介绍其超级播放器可以对视频生成 AI 字幕并提供双语对照，证明“边播放边生成字幕”的用户体验已经成熟。X 字幕在 App Store 的公开说明中同时提供本地视频、在线视频、实时字幕翻译、字幕编辑以及 SRT/TXT 导出，进一步验证了该产品方向。

本项目希望组合两类产品的优点：

- 保留夸克式的即时体验：无需先下载或完整上传视频，播放时直接生成；
- 提供字幕工具式的质量闭环：双语显示、稳定修订、会后校对和字幕文件导出；
- 不绑定特定浏览器：优先捕获用户指定应用的音频；
- 建立可替换 AI 服务层：根据地区、成本、语言和质量切换供应商。

公开宣传只能证明产品形态存在，不能证明其内部实现、准确率或延迟。本项目不得复制其他产品的代码、界面或受保护表达。

## 4. 三种实施方案对比

| 维度 | 方案 A：浏览器扩展 | 方案 B：Windows 指定应用捕获（推荐） | 方案 C：自带浏览器/播放器 |
|---|---|---|---|
| 在线网站覆盖 | 普通 Chrome/Edge 网页较好 | 只要应用能正常输出音频，覆盖通常最好 | 取决于内嵌浏览器兼容性 |
| 独立播放器覆盖 | 不支持 | 支持 | 只能支持自带播放器 |
| 夸克等独立应用 | 通常不支持 | 支持 | 需要用户改到软件内观看 |
| 音画同步 | 标签页内较好 | 需使用采集时钟和字幕稳定算法 | 最容易精确控制 |
| DRM/登录站点 | 页面能播放时通常可用，但扩展权限可能受限 | 不读取视频内容，只捕获已播放音频，兼容性相对较好 | DRM、登录和 WebView 兼容风险最高 |
| 开发难度 | 低至中 | 中至高，需 Windows 原生模块 | 中至高 |
| 隐私边界 | 当前标签页音频 | 用户明确选择的进程音频 | 软件内打开的内容 |
| 移动端延伸 | Android 浏览器支持不统一，iOS 更受限 | 后端协议可复用，采集层需平台原生实现 | Android/iOS 均可采用应用内播放 |
| 长期维护 | 受浏览器扩展 API 和商店审核影响 | 受 Windows 音频 API 和驱动差异影响 | 受浏览器内核和网站改版影响 |
| 主要风险 | 网站和扩展权限限制 | 进程识别、设备切换、独占模式和原生稳定性 | 网站登录、DRM、内核兼容和安全维护 |

### 4.1 方案 A：浏览器扩展

扩展直接捕获当前标签页音频，并在网页上注入字幕层。它适合快速验证普通网页视频，但无法自然覆盖夸克独立播放器、桌面媒体播放器或其他应用。不同浏览器的扩展权限和音频捕获能力也不完全一致。

适用情况：只服务 Chrome/Edge 普通网页，希望最快完成原型。

### 4.2 方案 B：Windows 指定应用音频捕获

桌面软件列出当前正在发声的应用。用户选择某个进程后，原生音频模块通过 WASAPI Process Loopback 捕获该进程及其子进程的音频。其他应用的通知声、音乐或会议声音不应混入当前会话。

这是第一阶段推荐方案，因为它最符合“不限定具体网站，不要求先下载视频”的目标。最低系统基线设置为 Windows 10 Build 20348，以使用进程级回环捕获能力。

需要注意：该方案不会绕过 DRM，也不读取、复制或解密原始视频。它只处理操作系统已经输出给用户的音频。仍需在用户协议中提醒用户遵守内容平台规则和著作权要求。

### 4.3 方案 C：自带浏览器/播放器

软件内置 WebView 和媒体播放器，能够更直接地读取播放、暂停和拖动事件，因此时间轴控制最好。但登录状态、Cookie、验证码、网页兼容、DRM、内核安全更新和移动端审核都会增加长期成本。

适用情况：后续为 iOS 提供“粘贴网址/分享链接后在应用内播放”的受控路径，或支持用户导入本地文件。不建议作为 Windows 第一阶段的唯一入口。

## 5. 推荐的 Windows MVP

### 5.1 功能范围

第一阶段包含：

- 自动列出正在发声的 Windows 应用；
- 手动选择目标进程，并捕获其进程树音频；
- 源语言自动识别或手动选择；
- 选择一个目标翻译语言；
- 实时显示译文或原文 + 译文；
- 临时字幕和稳定字幕使用不同状态，但默认不以突兀样式打扰用户；
- 悬浮字幕窗置顶、拖动、缩放、透明度、字号、背景、单双语切换和鼠标穿透；
- 暂停/继续字幕会话；
- 会话历史、简单文本校对；
- 导出 UTF-8 编码的 SRT、WebVTT 和 TXT；
- 显示网络、采集、识别、翻译和服务限流状态；
- 本机安全保存用户配置和密钥，或通过受控后端签发临时凭证。

第一阶段不包含：

- 绕过网站 DRM 或下载受保护视频；
- 同声配音、语音合成和原声消除；
- 多用户协作编辑；
- Windows、Android、iOS 同时发布；
- 对所有语言、网站、声卡或独占全屏应用作无条件兼容承诺。

### 5.2 建议技术架构

```text
┌──────────────── Windows Desktop Client ────────────────┐
│                                                        │
│  控制面板          悬浮字幕窗           字幕编辑器      │
│       │                 ▲                    ▲         │
│       ▼                 │                    │         │
│  会话控制器 ─────→ 字幕状态仓库 ─────→ 导出服务         │
│       │                 ▲                              │
│       ▼                 │                              │
│  WASAPI 进程捕获 → PCM/VAD/分段 → ProviderAdapter     │
└──────────────────────────────┬─────────────────────────┘
                               │ WebSocket / HTTPS
                               ▼
┌──────────────── Cloud Provider / Optional Backend ─────┐
│  鉴权 → 实时 ASR → 实时翻译 → 增量事件/稳定事件         │
└────────────────────────────────────────────────────────┘
```

客户端采用跨平台 UI 层加 Windows 原生音频模块。UI 技术可以在实施阶段选择成熟的桌面跨平台框架，但音频采集必须封装为明确的 Windows 平台接口，不能依赖未经验证的通用录音插件。

如面向正式用户发布，推荐增加一个轻量业务后端：长期云密钥只保存在服务端，客户端登录后获取短期会话凭证。仅用于个人原型时，才允许将密钥加密保存在 Windows Credential Manager 等本机安全存储中。

### 5.3 云服务策略

首接候选为腾讯云实时语音识别与翻译，原因包括：

- 国内网络环境下通常更易稳定接入；
- 官方提供实时流式识别、VAD、热词以及原文/译文结果；
- 官方资料覆盖多种源语言和翻译目标语言；
- 有实时同传和增量更新示例可供验证。

正式实施前必须使用项目自己的腾讯云账号完成技术预研，确认所选具体产品是否允许直接推送客户端采集的 PCM 音频，是否要求进入 RTC 房间，以及当前的计费、并发、开通资格、鉴权方式和语言组合。不同腾讯云产品线的接口不可混用。

供应商抽象层必须允许后续接入 OpenAI 等其他服务。OpenAI 官方当前提供面向实时低延迟转写的流式模型和实时转写端点，但账号可用性、地区网络、计费和目标语言翻译链路仍需单独验证。

所有密钥通过环境变量、操作系统安全存储或服务端密钥管理加载。仓库只提供 `.env.example`，不得提交真实密钥。

## 6. 核心数据模型与接口

以下结构为逻辑接口，具体编程语言可在实施阶段映射为类型或类。

### 6.1 `AudioSource`

```ts
interface AudioSource {
  sourceId: string;
  processId: number;
  includeProcessTree: boolean;
  applicationName: string;
  executablePath?: string;
  sampleRate: number;
  channels: number;
  isActive: boolean;
}
```

### 6.2 `AudioChunk`

```ts
interface AudioChunk {
  sessionId: string;
  sequence: number;
  startTimeMs: number;
  durationMs: number;
  sampleRate: number;
  channels: 1;
  encoding: "pcm_s16le";
  payload: ArrayBuffer;
  discontinuity: boolean;
}
```

约束：

- `sequence` 在单个会话内单调递增；
- `startTimeMs` 使用会话单调时钟，不使用可能跳变的系统墙上时间；
- 检测到丢帧、设备切换或暂停恢复时设置 `discontinuity`；
- 上传前统一转换成供应商要求的采样率、单声道和编码格式。

### 6.3 `SubtitleSegment`

```ts
interface SubtitleSegment {
  segmentId: string;
  sessionId: string;
  revision: number;
  startTimeMs: number;
  endTimeMs?: number;
  sourceLanguage: string;
  targetLanguage: string;
  sourceText: string;
  translatedText: string;
  state: "partial" | "stable";
  confidence?: number;
}
```

合并规则：

- 相同 `segmentId` 只接受更高 `revision`；
- `partial` 可以覆盖同片段较旧的 `partial`；
- `stable` 可以覆盖 `partial`；
- 已稳定片段不接受后续临时结果覆盖；
- 供应商改变片段 ID 时，由适配器先转换为统一语义；
- UI 对高频修订做 100–250 ms 合并刷新，减少闪烁。

### 6.4 `ProviderAdapter`

```ts
interface ProviderAdapter {
  connect(config: ProviderSessionConfig): Promise<void>;
  sendAudio(chunk: AudioChunk): Promise<void>;
  onSubtitle(listener: (segment: SubtitleSegment) => void): Unsubscribe;
  onStatus(listener: (status: ProviderStatus) => void): Unsubscribe;
  finish(): Promise<void>;
  reconnect(resume: ResumeContext): Promise<void>;
  dispose(): Promise<void>;
}
```

统一错误类型至少包括：鉴权失败、余额或配额不足、语言不支持、限流、网络超时、服务端异常、音频格式错误和会话过期。

### 6.5 会话与导出

客户端本地保存稳定字幕、必要的临时恢复状态和用户校对结果。默认不保存原始 PCM，也不自动上传完整视频。若用户选择保存调试音频，必须单独授权，并明确保存位置与删除入口。

导出规则：

- SRT 使用 `HH:MM:SS,mmm`；
- VTT 使用 `HH:MM:SS.mmm`；
- 文件统一为 UTF-8；
- 时间段不得倒序，重叠片段在导出前进行最小化修正；
- 双语字幕默认原文在上、译文在下；
- TXT 可选择仅译文、仅原文或双语。

## 7. 关键技术问题与处理策略

### 7.1 音频捕获与进程选择

浏览器和部分播放器采用多进程架构，因此需要捕获目标进程及其子进程。应用列表应优先显示正在产生音频的顶层应用，而不是要求普通用户理解 PID。

需要处理：

- 目标进程重启或创建新的媒体子进程；
- 默认输出设备切换；
- 蓝牙设备采样率变化；
- 目标应用暂时静音；
- 独占全屏或特殊音频驱动；
- 捕获模块收到静音数据但进程仍然存在。

### 7.2 人声、背景音乐与多人对话

MVP 不承诺完全分离人声和背景音乐。优先依赖云端 ASR 的抗噪能力，并在本地加入轻量 VAD，避免将长静音持续计费。后续可以评估人声增强或源分离，但它们会增加计算量和延迟。

多人对话第一阶段只保证连续字幕，不承诺准确说话人区分。若供应商返回可靠的说话人信息，可作为可选字段保留。

### 7.3 断句和翻译修订

原文识别和翻译的稳定时机不同。系统不能把每个临时词都立即固化为 SRT，而应维护“当前活动片段”和“已稳定片段”。极速模式优先展示临时译文；句末、VAD 停顿或供应商稳定事件触发固化。

### 7.4 网络抖动与背压

音频发送队列必须有上限。如果云端处理速度持续落后：

1. 短暂抖动时保留有限缓冲并重连；
2. 超过缓冲上限时明确标记音频不连续；
3. 不允许队列无限增长导致字幕延迟越来越大；
4. UI 显示“网络较慢，字幕可能遗漏”；
5. 不得用猜测文本填补缺失语音。

### 7.5 密钥与隐私

- 正式产品不向客户端分发长期云密钥；
- 日志默认不记录音频、完整字幕、令牌或签名；
- 用户开始捕获前必须明确选择应用并看到采集状态；
- 默认不捕获麦克风，也不捕获未选择的其他应用；
- 提供会话删除和本地数据清理能力；
- 隐私政策要说明音频片段会发送给所选云服务进行处理。

### 7.6 版权与平台规则

软件只为用户提供实时辅助理解和字幕工具，不应提供破解、去 DRM、盗链或未授权视频下载能力。用户应确认其有权处理相关内容。正式发布前应由法律专业人士审查隐私政策、用户协议、第三方云服务条款和目标市场的数据合规要求。

## 8. 移动端演进方案

### 8.1 可复用部分

Windows、Android 和 iOS 复用：

- `AudioChunk` 与 `SubtitleSegment` 协议；
- 云服务适配器的服务端部分；
- 会话、账号、语言配置和字幕导出逻辑；
- 字幕合并、修订和稳定算法；
- 大部分非平台相关 UI 和状态管理设计。

音频采集不能假设跨平台直接复用，必须按系统实现。

### 8.2 Android

Android 10 起提供 AudioPlaybackCapture。用户需要授权 MediaProjection，而且只有符合系统规则、没有禁止捕获的媒体应用音频才可被采集。因此后续 Android 版本可以提供：

1. 其他 App 音频捕获；
2. 应用内网页或本地播放器；
3. 捕获失败时提示目标 App 不允许采集，并引导使用应用内播放。

不得宣传为可以捕获所有 Android App。

### 8.3 iOS

iOS 对跨应用音频和屏幕内容限制更严格，ReplayKit 也存在内容兼容限制。因此 iOS 推荐主路径为：

- 从浏览器或其他 App 通过分享菜单把视频网址发送到本软件；
- 在应用内网页播放器或媒体播放器中播放并生成字幕；
- 导入用户有权处理的本地视频；
- 对系统允许的录屏/广播能力仅作为补充，并通过真机审核验证。

不得承诺像 Windows 一样任意选择第三方 App 并捕获其音频。

## 9. 测试计划与验收标准

### 9.1 音频捕获测试

- 分别选择夸克、Chrome、Edge 和至少一个本地播放器；
- 同时让其他应用播放声音，确认不会混入字幕会话；
- 覆盖浏览器多进程、标签切换、进程重启和暂停恢复；
- 测试扬声器、有线耳机、USB 声卡和蓝牙耳机切换；
- 验证静音、无音频流和独占全屏场景不会导致崩溃。

### 9.2 语言与内容测试

- 英语、日语、韩语分别翻译为中文；
- 中文翻译为英语；
- 正常语速、快速对白、背景音乐、多人对话、口音和长静音；
- 人名、地名和专业术语通过热词配置验证改善效果；
- 对暂不支持的语言给出明确提示。

### 9.3 延迟测试

使用固定测试视频和可重复网络条件记录：

- 人声实际开始时间；
- 首批临时原文时间；
- 首批临时译文时间；
- 原文稳定时间；
- 译文稳定时间；
- P50、P95、最大延迟和丢失片段数。

延迟必须由程序埋点和统一素材测量，不能只凭观看感受判断。

### 9.4 字幕状态测试

- 临时修订不会生成重复字幕；
- 修订版本不会倒退；
- UI 不会因逐字符更新持续闪烁；
- 稳定字幕不会被旧的临时结果覆盖；
- 暂停或拖动视频后，旧时间轴不会污染新字幕；
- 网络断开后不生成虚假字幕。

### 9.5 故障测试

- 断网、弱网、WebSocket 中断和恢复；
- API Key 错误、服务未开通、余额不足和限流；
- 云服务返回格式错误或未知状态；
- 本地发送队列达到上限；
- 目标进程退出、重启或不再输出声音；
- 软件异常退出后的会话恢复。

### 9.6 导出测试

- SRT、VTT 和 TXT 均能以 UTF-8 正确打开；
- 中文和其他语言不乱码；
- 时间戳格式正确且不倒序；
- 重叠片段得到合理修正；
- 双语换行和长句换行可读；
- 用户校对后的文本进入导出文件。

### 9.7 MVP 验收标准

满足以下条件才能称为 MVP 完成：

1. 用户可以选择目标应用并独立捕获其声音；
2. 至少完成英语、日语、韩语到中文，以及中文到英语的真实云端测试；
3. 支持实时译文和双语字幕切换；
4. 普通网络下字幕连续可读，延迟测量报告完整；
5. 支持暂停、恢复、断线提示和至少一次自动重连；
6. 会话可以校对并导出有效的 SRT、VTT 和 TXT；
7. 没有将真实 API Key 写入仓库或客户端明文配置；
8. 未达到的语言准确率、延迟或兼容性指标被如实记录。

## 10. 实施阶段与里程碑

### 阶段 0：技术验证

- 验证 Windows 指定进程及进程树的 PCM 捕获；
- 验证夸克、Chrome、Edge 和本地播放器；
- 使用腾讯云账号验证实际实时接口、鉴权、语言、增量事件和费用；
- 记录真实延迟和识别质量；
- 通过后再确定最终 UI 框架和商业后端方案。

交付物：音频捕获原型、云端链路原型、验证报告和风险清单。

### 阶段 1：字幕 MVP

- 完成进程选择、会话控制和音频流；
- 完成供应商适配器和字幕状态合并；
- 完成悬浮字幕窗、单双语切换和错误状态；
- 完成 SRT/VTT/TXT 导出；
- 完成基础安全、日志和自动化测试。

交付物：可安装的 Windows 内测版。

### 阶段 2：质量与可用性

- 字幕校对、历史会话和热词；
- 极速、平衡、高质量模式；
- 音频设备切换和异常恢复；
- 性能、内存、长时间运行和多语言回归；
- 增加第二家云服务验证，确认适配器抽象有效。

交付物：公开测试版和完整质量报告。

### 阶段 3：移动端

- 优先开发 Android AudioPlaybackCapture 版本；
- 对禁止采集的 App 提供应用内播放降级路径；
- iOS 采用分享链接、应用内网页和本地文件路径；
- 真机验证系统权限、后台行为、商店审核和云服务成本。

## 11. 主要风险清单

| 风险 | 影响 | 应对策略 |
|---|---|---|
| 用户认为“无延迟”等于严格零延迟 | 预期无法满足 | 使用可测量的首字、P50、P95 指标，并展示临时字幕 |
| 浏览器多进程导致漏抓音频 | 无字幕或不完整 | 捕获进程树，监控新子进程和静音状态 |
| 云服务接口与 RTC 产品线不匹配 | 无法直接发送 PCM | 在阶段 0 用真实账号完成最小链路验证 |
| 临时结果反复修订 | 字幕跳动、重复 | 统一片段 ID、版本号、稳定状态和 UI 合并刷新 |
| 网络处理速度落后于播放速度 | 延迟不断累积 | 有界队列、背压、丢失标记和明确提示 |
| 背景音乐与多人对白 | 准确率下降 | 选择高质量 ASR、热词、后续评估人声增强 |
| 云密钥泄露 | 费用和安全风险 | 正式产品使用服务端临时凭证，本机使用安全存储 |
| 供应商价格或语言能力变化 | 成本和兼容性变化 | 适配器抽象、配置化能力表和定期复核 |
| Android 目标 App 禁止采集 | 无法获得音频 | 应用内播放降级，不承诺覆盖所有 App |
| iOS 跨应用捕获受限 | 无法复制 Windows 体验 | 分享链接和应用内播放器作为主路径 |
| 内容版权或平台条款 | 法律与上架风险 | 不下载、不解密、不绕过 DRM，发布前法律审查 |

## 12. 最终建议

第一阶段应选择方案 B：Windows 指定应用音频捕获。它最接近“用户在任意常见浏览器或播放器看在线视频，软件马上生成翻译字幕”的目标，同时不要求下载视频，也不把产品锁死在某一个网站或浏览器扩展中。

实施顺序应为：

1. 先证明夸克、Chrome、Edge 的指定进程音频能稳定捕获；
2. 再用真实腾讯云账号验证流式识别和翻译链路；
3. 达到基本延迟目标后再建设完整桌面 UI；
4. MVP 直接包含双语字幕和 SRT/VTT/TXT 导出；
5. 完成第二供应商验证后，再启动 Android；
6. iOS 使用应用内播放路线，不承诺任意第三方 App 音频捕获。

这个顺序能最早暴露真正的高风险部分——系统音频捕获、云接口形态和实际延迟——并避免先投入大量界面开发后才发现底层链路不满足要求。

## 13. 官方参考资料

以下资料用于确认平台能力和产品可行性。接口、价格、语言和系统要求可能更新，正式实施前应再次核对。

### Windows

- [Microsoft：Application loopback audio capture sample](https://learn.microsoft.com/en-us/samples/microsoft/windows-classic-samples/applicationloopbackaudio-sample/)
- [Microsoft：ActivateAudioInterfaceAsync](https://learn.microsoft.com/en-us/windows/win32/api/mmdeviceapi/nf-mmdeviceapi-activateaudiointerfaceasync)
- [Microsoft：AUDIOCLIENT_ACTIVATION_TYPE](https://learn.microsoft.com/en-us/windows/win32/api/audioclientactivationparams/ne-audioclientactivationparams-audioclient_activation_type)
- [Microsoft：Loopback Recording](https://learn.microsoft.com/en-us/windows/win32/coreaudio/loopback-recording)

### 云端实时识别与翻译

- [腾讯云：AI 实时转录功能介绍](https://cloud.tencent.com/document/product/647/126311)
- [腾讯云：通过实时语音翻译实现同声传译](https://cloud.tencent.com/document/product/1093/130882)
- [腾讯云：AI 智能语音计费说明](https://cloud.tencent.com/document/product/647/111976)
- [OpenAI：GPT Live Transcribe](https://developers.openai.com/api/docs/models/gpt-live-transcribe)
- [OpenAI：GPT Transcribe](https://developers.openai.com/api/docs/models/gpt-transcribe)

### 移动平台

- [Android Developers：Capture video and audio playback](https://developer.android.com/media/platform/av-capture)
- [Android Developers：AudioPlaybackCaptureConfiguration](https://developer.android.com/reference/android/media/AudioPlaybackCaptureConfiguration)
- [Apple Developer：ReplayKit](https://developer.apple.com/documentation/ReplayKit)

### 产品形态参考

- [夸克官网：超级播放器与视频翻译](https://www.quark.cn/)
- [Apple App Store：X 字幕](https://apps.apple.com/cn/app/x%E5%AD%97%E5%B9%95-%E8%A7%86%E9%A2%91%E5%AE%9E%E6%97%B6%E7%BF%BB%E8%AF%91-%E8%87%AA%E5%8A%A8%E4%B8%BA%E8%A7%86%E9%A2%91%E6%B7%BB%E5%8A%A0%E5%AD%97%E5%B9%95-%E5%AD%97%E5%B9%95%E7%BC%96%E8%BE%91%E5%AF%BC%E5%87%BA/id6670429646)

---

本文件是立项与技术方案文档，不代表已完成真实供应商接入、浏览器兼容测试、性能测试或法律审查。所有性能和兼容性结论都应以阶段 0 的实机验证结果为准。
